# Ghost Features

An agent that detects orphaned ML features — feature-store entries whose upstream warehouse columns have been renamed, dropped, or changed without the feature store being updated — using DataHub's schema and ML lineage metadata.